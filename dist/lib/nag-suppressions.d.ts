import { NagPackSuppression } from 'cdk-nag';
/**
 * Global suppressions that apply across all stacks
 * Only add suppressions here with clear justification
 */
export declare const globalSuppressions: NagPackSuppression[];
